import React from 'react';

export default function Login(){
    return(
        <main className="container-fluid bg-secondary text-center">
            <div>login displayed</div>
        </main>
    );
}